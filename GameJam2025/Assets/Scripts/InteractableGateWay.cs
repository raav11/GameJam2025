using UnityEngine;

public class InteractableGateWay : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public void TestingPurposes()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
